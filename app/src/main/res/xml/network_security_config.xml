<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Audiobookshelf is often hosted on private LAN IPs. Public HTTP is blocked in ServerUrlPolicy. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
